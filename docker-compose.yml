version: "3.9"

services:
  bot:
    build: .
    container_name: english-speaking-bot
    env_file:
      - .env
    command: python main.py
    volumes:
      - ./bot.db:/app/bot.db
      - ./temp_audio:/app/temp_audio
    restart: unless-stopped

